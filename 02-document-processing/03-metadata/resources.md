# Resources - Metadata

Only things I actually read or watched. No link dumps.

| Resource | Type | Link | Why it was useful |
| --- | --- | --- | --- |
| Document Loaders (LangChain playlist) | Course video | *TODO - paste the video URL* | Covered metadata only as the second field of the `Document` object, and what each loader populates. |

## Best explanation I found

Nothing dedicated yet - metadata appeared as a side note to document loading.
The useful observation so far is that metadata is the only part of a document
that survives the pipeline unchanged: text gets split and meaning gets
compressed into a vector, but `source` and `page` pass straight through.

## Explanations that did NOT help

n/a - not studied properly yet.

<!-- Add sources here when I study this topic directly. -->
